# ZK-SNARK Video Steganography - Dependencies

## Core Dependencies (v2.0 - existing)
numpy>=1.21.0          # Array operations, DCT coefficients
# Note: No additional Python packages required for v2.0

## New Dependencies for v3.0 Upgrade

### Phase 1: YUV + DWT (Weeks 1-3)
# pywavelets>=1.3.0      # Haar DWT implementation (alternative to custom)
opencv-python>=4.5.0    # YUV conversion, image processing
scikit-image>=0.19.0    # Texture analysis, Laplacian variance

### Phase 2: RC4 + Context-Aware (Weeks 4-6)
# pycryptodome>=3.15.0  # RC4 cipher (if not implementing from scratch)
scipy>=1.7.0            # Signal processing for context analysis

### Phase 3: LDPC + Interleaving (Weeks 7-9)
# pyldpc>=0.7.9         # LDPC codec (alternative to custom implementation)
# Note: Consider implementing LDPC from scratch for better control

### Phase 4: Testing & Validation (Weeks 10-12)
pytest>=7.0.0           # Unit testing
pytest-cov>=3.0.0       # Code coverage
matplotlib>=3.5.0       # Visualization for DWT/attention maps
pillow>=9.0.0           # Image I/O for testing

## Development Tools
black>=22.0.0           # Code formatting
flake8>=4.0.0           # Linting
mypy>=0.950             # Type checking

## Optional (Performance)
# numba>=0.55.0         # JIT compilation for DWT/LDPC
# cupy>=10.0.0          # GPU acceleration (CUDA required)

## JavaScript/Node.js Dependencies (ZK-SNARK circuits)
# These are installed via npm in circuits/ directory
# - snarkjs@0.7.0       # Groth16 prover/verifier
# - circomlib@2.0.5     # SHA256 circuits
